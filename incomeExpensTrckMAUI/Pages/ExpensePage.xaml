<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:mct="clr-namespace:CommunityToolkit.Maui.Behaviors;assembly=CommunityToolkit.Maui"
             xmlns:styles="clr-namespace:incomeExpensTrckMAUI.Resources.Styles"
             xmlns:views="clr-namespace:incomeExpensTrckMAUI.Views"
             x:Class="incomeExpensTrckMAUI.Pages.ExpensePage"
             Title="ExpensePage"
             Shell.BackgroundColor="White"
             Shell.TitleColor="Black">
    <ContentPage.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <styles:ExpensePageStyles />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ContentPage.Resources>
    <Shell.TitleView>
        <StackLayout Style="{StaticResource PageTitleStyle}">
            <Label Text="{Binding Title}" Style="{StaticResource TitleLabelStyle}"/>
        </StackLayout>
    </Shell.TitleView>
    <VerticalStackLayout Style="{StaticResource MainLayoutStyle}">
        <!-- Expense List here -->
        <views:ExpenseListView/>
        <!-- Add Expense Button -->
        <Button Text="Add Expense" 
                Style="{StaticResource AddExpenseButtonStyle}" 
                 />
    </VerticalStackLayout>
</ContentPage>